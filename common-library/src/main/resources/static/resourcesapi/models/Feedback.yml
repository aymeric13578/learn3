Feedback:
  type: object
  properties:
    id:
      $ref: './UUID.yml'
    user:
      $ref: './User.yml'
    course:
      $ref: './Course.yml'
    feedbackType:
      type: string
    feedbackData:
      type: string
    resolved:
      type: boolean
    comment:
      type: string
