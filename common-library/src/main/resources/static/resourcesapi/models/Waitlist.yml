Waitlist:
  type: object
  properties:
    id:
      $ref: './UUID.yml'
    user:
      $ref: '#/components/schemas/User'
    course:
      $ref: '#/components/schemas/Course'
    dateAdded:
      type: string
      format: date-time
