AuthLoginData:
  type: object
  required:
    - email
    - password
  properties:
    email:
      type: string
      format: email
      description: "Email of the user"
      example: "john.doe@example.com"
    password:
      type: string
      description: "Password of the user"
      example: "P@ssw0rd123"
