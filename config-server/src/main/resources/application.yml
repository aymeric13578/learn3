server:
  port: 8888
spring:
  profiles:
    active: native
  application:
    name: config-server
  cloud:
    compatibility-verifier:
      enabled: false
    config:
      server:
        native:
          search-locations: classpath:/configuration